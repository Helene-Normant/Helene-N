
const ButtonLarge = ({ className, children, type }) => { 
    return (
        <button
            className={className}
            children={children}
            type={type}
        />
  )
};

export default ButtonLarge;